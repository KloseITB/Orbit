package it.unipv.posfw.orbit.view.UI.prefab;

import java.awt.Color;

import javax.swing.JButton;

public class Prefab {

	/*
		protected JButton OrbitButton(String text, int width, int height) {
		JButton button = new JButton(text);
		button.setFocusable(false);
		
		button.setBounds((WINDOW_WIDTH - width) / 2, (WINDOW_HEIGHT - height) / 2, width, height);
		button.setBackground(BUTTON_PURPLE);
		button.setForeground(Color.WHITE);
		
		
		
		return button;
	}
	*/
}
